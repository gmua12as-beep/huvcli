List project files (respects .gitignore via `git ls-files` when available, otherwise walks the tree and skips common build/cache directories).

Use this to get a quick lay of the land at the start of a session. For targeted lookups, `glob` is usually a better fit.
