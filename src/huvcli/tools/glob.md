Match files by glob pattern relative to the project root, with `**` recursion.

Examples: `src/**/*.tsx`, `tests/test_*.py`, `*.md`.

Faster than `list_files` when you know the shape of what you're looking for.
